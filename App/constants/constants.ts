import {Dimensions} from 'react-native';

export const gWindowWidth = Dimensions.get('window').width;
export const gWindowHeight = Dimensions.get('window').height;

export const ITEM_BORDER_RADIUS = 10;
